import React from 'react';
import FontAwesomeIcon from '@fortawesome/react-fontawesome'
import { faEdit } from '@fortawesome/fontawesome-free-solid'

export const Edit = (props) => <FontAwesomeIcon icon={faEdit} {...props} />
